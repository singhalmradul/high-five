spring:
  datasource:
    url: jdbc:postgresql://localhost/high-five
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 3000
